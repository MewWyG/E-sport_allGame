// ─────────────────────────────────────────────────────────────────────
//  🔧 GAME CONFIG — ปรับค่าตรงนี้เพื่อเปลี่ยน gameplay
// ─────────────────────────────────────────────────────────────────────
/** จำนวนกระสุนต่อแม็ก — เพิ่ม/ลดได้ที่นี่ */
export const MAGAZINE_SIZE = 30;
/** อัตรายิง (ms ระหว่างนัด) — น้อย = ยิงเร็ว */
export const FIRE_RATE_MS = 105;
/** รัศมีของเป้า (px) — กว้างขึ้น = ตีโดนง่ายขึ้น */
export const TARGET_RADIUS_PX = 58;
/** รัศมีโซน Perfect ตรงกลางเป้า (px) */
export const PERFECT_RADIUS_PX = 24;
/** เวลาที่รอผู้เล่นกดเริ่มยิงในเฟส READY (วินาที) */
export const READY_TIMEOUT_SEC = 10;
